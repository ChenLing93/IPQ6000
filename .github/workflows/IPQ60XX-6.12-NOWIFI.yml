name: IPQ60XX-6.12-NOWIFI

on:
  workflow_dispatch:
  schedule:
    - cron: 0 19 * * *

env:
  REPO_URL: https://github.com/LiBwrt/openwrt-6.x.git
  REPO_BRANCH: main-nss
  CONFIG_FILE: configs/ipq60xx-6.12-nowifi.config
  DIY_SCRIPT: libwrt.sh
  CLASH_KERNEL: amd64
  CACHE_TOOLCHAIN: true
  UPLOAD_BIN_DIR: false
  FIRMWARE_RELEASE: true
  FIRMWARE_TAG: IPQ60XX-6.12-NOWIFI
  TZ: Asia/Shanghai

jobs:
  Build:
    runs-on: ubuntu-latest

    steps:
    - name: Check Server Performance(检查服务器性能)
      run: |
        echo "警告⚠"
        echo "分配的服务器性能有限，若选择的插件过多，务必注意CPU性能！"
        echo -e "已知CPU型号（降序）：7763，8370C，8272CL，8171M，E5-2673 \n"
        echo "--------------------------CPU信息--------------------------"
        echo "CPU物理数量：$(cat /proc/cpuinfo | grep "physical id" | sort | uniq | wc -l)"
        echo -e "CPU核心信息：$(cat /proc/cpuinfo | grep name | cut -f2 -d: | uniq -c) \n"
        echo "--------------------------内存信息--------------------------"
        echo "已安装内存详细信息："
        echo -e "$(sudo lshw -short -C memory | grep GiB) \n"
        echo "--------------------------硬盘信息--------------------------"
        echo "硬盘数量：$(ls /dev/sd* | grep -v [1-9] | wc -l)"
        df -hT

    - name: Initialization Environment(初始化环境)
      env:
        DEBIAN_FRONTEND: noninteractive
      run: |
        sudo bash -c 'bash <(curl -sL https://build-scripts.immortalwrt.org/init_build_environment.sh)'
        sudo -E apt -yqq install dos2unix
        sudo -E apt -yqq install libfuse-dev
        sudo -E apt -yqq autoremove --purge
        sudo -E apt -yqq autoclean
        sudo -E apt -yqq clean
        sudo -E systemctl daemon-reload
        sudo timedatectl set-timezone "$TZ"

    - name: Combine Disks(合并磁盘)
      uses: easimon/maximize-build-space@master
      with:
        swap-size-mb: 1024
        temp-reserve-mb: 100
        root-reserve-mb: 1024

    - name: Checkout
      uses: actions/checkout@main

    - name: Clone Source Code(克隆源代码)
      run: |
        df -hT $GITHUB_WORKSPACE
        git clone --depth 1 -b $REPO_BRANCH $REPO_URL openwrt
        cd openwrt
        echo "OPENWRT_PATH=$PWD" >> $GITHUB_ENV
        VERSION_INFO=$(git show -s --date=short --format="作者: %an时间: %cd内容: %shash: %H")
        echo "VERSION_INFO=$VERSION_INFO" >> $GITHUB_ENV
        VERSION_KERNEL=$(grep -oP 'LINUX_KERNEL_HASH-\K[0-9]+\.[0-9]+\.[0-9]+' target/linux/generic/kernel-6.12)
        echo "VERSION_KERNEL=$VERSION_KERNEL" >> $GITHUB_ENV

    - name: Generate Variables(生成变量)
      run: |
        cp $CONFIG_FILE $OPENWRT_PATH/.config
        cd $OPENWRT_PATH
        make defconfig > /dev/null 2>&1
        SOURCE_REPO="$(echo $REPO_URL | awk -F '/' '{print $(NF)}')"
        echo "SOURCE_REPO=$SOURCE_REPO" >> $GITHUB_ENV
        DEVICE_TARGET=$(cat .config | grep CONFIG_TARGET_BOARD | awk -F '"' '{print $2}')
        echo "DEVICE_TARGET=$DEVICE_TARGET" >> $GITHUB_ENV
        DEVICE_SUBTARGET=$(cat .config | grep CONFIG_TARGET_SUBTARGET | awk -F '"' '{print $2}')
        echo "DEVICE_SUBTARGET=$DEVICE_SUBTARGET" >> $GITHUB_ENV

    - name: Cache Toolchain(缓存工具链)
      if: env.CACHE_TOOLCHAIN == 'true'
      uses: HiGarfield/cachewrtbuild@main
      with:
        ccache: false
        mixkey: ${{ env.SOURCE_REPO }}-${{ env.REPO_BRANCH }}-${{ env.DEVICE_TARGET }}-${{ env.DEVICE_SUBTARGET }}
        prefix: ${{ env.OPENWRT_PATH }}

    - name: Fix mbedtls GCC 14 Compatibility(修复mbedtls GCC 14兼容性)
      run: |
        cd $OPENWRT_PATH
        
        # 创建本地覆盖文件，禁用 FORTIFY_SOURCE
        mkdir -p include
        cat > include/local.mk << 'EOF'

        TARGET_CFLAGS += -U_FORTIFY_SOURCE -D_FORTIFY_SOURCE=0
        TARGET_CXXFLAGS += -U_FORTIFY_SOURCE -D_FORTIFY_SOURCE=0
        EOF
        
        # 修复全局 hardened.mk
        if [[ -f "include/hardened.mk" ]]; then
            sed -i 's/-D_FORTIFY_SOURCE=1/-D_FORTIFY_SOURCE=0/g' include/hardened.mk
            sed -i 's/-D_FORTIFY_SOURCE=2/-D_FORTIFY_SOURCE=0/g' include/hardened.mk
            sed -i 's/-D_FORTIFY_SOURCE=3/-D_FORTIFY_SOURCE=0/g' include/hardened.mk
        fi
        
        # 修复 mbedtls Makefile（确保 CMake 使用正确的编译标志）
        MBEDTLS_MAKEFILE="package/libs/mbedtls/Makefile"
        if [[ -f "$MBEDTLS_MAKEFILE" ]]; then
            # 移除已知的编译警告选项
            sed -i 's/-Wno-unterminated-string-initialization//g' "$MBEDTLS_MAKEFILE"
            sed -i 's/-Wno-shift-count-overflow//g' "$MBEDTLS_MAKEFILE"
            sed -i 's/-Wno-array-bounds//g' "$MBEDTLS_MAKEFILE"
            
            # 确保添加 -Wno-error 以避免警告变成错误
            if ! grep -q "CMAKE_OPTIONS.*-Wno-error" "$MBEDTLS_MAKEFILE"; then
                sed -i '/CMAKE_OPTIONS +=/ s/$/ -Wno-error/' "$MBEDTLS_MAKEFILE"
            fi
            
            # 添加 FORTIFY_SOURCE 禁用标志
            if ! grep -q "U_FORTIFY_SOURCE" "$MBEDTLS_MAKEFILE"; then
                sed -i '/CMAKE_OPTIONS +=/a \\ -U_FORTIFY_SOURCE -D_FORTIFY_SOURCE=0' "$MBEDTLS_MAKEFILE"
            fi
        fi

    - name: Install Feeds(安装feeds)
      run: |
        cd $OPENWRT_PATH
        # 添加官方 packages 源（修复 libparted、boost-system、python3-pysocks 等依赖缺失）
        if ! grep -q "src-git packages" feeds.conf.default 2>/dev/null; then
          echo "src-git packages https://github.com/openwrt/packages.git;openwrt-23.05" >> feeds.conf.default
        fi
        # 添加官方 luci 源
        if ! grep -q "src-git luci" feeds.conf.default 2>/dev/null; then
          echo "src-git luci https://github.com/openwrt/luci.git;openwrt-23.05" >> feeds.conf.default
        fi
        ./scripts/feeds update -a
        ./scripts/feeds install -a

    - name: Load Custom Configuration(加载自定义配置)
      run: |
        [ -e files ] && mv files $OPENWRT_PATH/files
        [ -e $CONFIG_FILE ] && mv $CONFIG_FILE $OPENWRT_PATH/.config
        chmod +x $DIY_SCRIPT
        cd $OPENWRT_PATH
        $GITHUB_WORKSPACE/$DIY_SCRIPT

    # 👇 添加完整的修复步骤 👇
    - name: 修复所有编译问题
      run: |
        cd openwrt
        
        echo "=================================="
        echo "开始修复所有编译问题"
        echo "=================================="
        
        # ==========================================
        # 步骤 1：修复配置文件格式
        # ==========================================
        echo ""
        echo "[步骤 1/4] 修复配置文件格式..."
        
        if [[ -f "./.config" ]]; then
            # 转换换行符（CRLF -> LF）
            sed -i 's/\r$//' .config
            
            # 移除前导空白
            sed -i 's/^[ \t]*//' .config
            
            # 清理空行
            sed -i '/^$/d' .config
            
            echo "  ✓ 配置文件格式已修复"
        fi
        
        # ==========================================
        # 步骤 2：修复缺失的依赖包
        # ==========================================
        echo ""
        echo "[步骤 2/4] 修复缺失的依赖包..."
        
        # 2.1 确保 Nikki 包正确下载
        if [[ ! -d "package/nikki" ]]; then
            rm -rf package/nikki package/luci-app-nikki
            git clone --depth=1 --single-branch --branch main "https://github.com/nikkinikki-org/OpenWrt-nikki.git" package/nikki
            if [[ -d "package/nikki/luci-app-nikki" ]]; then
                cp -rf package/nikki/luci-app-nikki ./package/
            fi
            echo "  ✓ Nikki 包已下载"
        fi
        
        # 2.2 更新 feeds
        if [[ -f "feeds.conf.default" ]]; then
            ./scripts/feeds update -a || true
            ./scripts/feeds install -a || true
            echo "  ✓ feeds 已更新"
        fi
        
        # 2.3 确保 Python3 包在 .config 中启用
        if [[ -f "./.config" ]]; then
            for pkg in python3 python3-light python3-base python3-pysocks python3-unidecode; do
                if ! grep -q "CONFIG_PACKAGE_${pkg}=y" .config; then
                    echo "CONFIG_PACKAGE_${pkg}=y" >> .config
                fi
            done
            echo "  ✓ Python3 包已添加"
        fi
        
        # 2.4 禁用可能导致编译失败的包
        if [[ -f "./.config" ]]; then
            # 禁用 trojan-plus（依赖 boost-system）
            sed -i 's/^CONFIG_PACKAGE_trojan-plus=y/# CONFIG_PACKAGE_trojan-plus is not set/' .config
            
            # 禁用 fatresize（依赖 libparted）
            sed -i 's/^CONFIG_PACKAGE_fatresize=y/# CONFIG_PACKAGE_fatresize is not set/' .config
            
            # 如果 shadowsocks-libev 不存在，禁用 luci-app-ssr-plus
            if [[ ! -d "package/shadowsocks-libev" ]]; then
                sed -i 's/^CONFIG_PACKAGE_luci-app-ssr-plus=y/# CONFIG_PACKAGE_luci-app-ssr-plus is not set/' .config
            fi
            
            echo "  ✓ 问题包已禁用"
        fi
        
        # ==========================================
        # 步骤 3：强制修复 mbedtls GCC 14 编译错误
        # ==========================================
        echo ""
        echo "[步骤 3/4] 强制修复 mbedtls GCC 14 编译错误..."
        
        MBEDTLS_DIR="package/libs/mbedtls"
        MBEDTLS_MAKEFILE="$MBEDTLS_DIR/Makefile"
        
        if [[ -f "$MBEDTLS_MAKEFILE" ]]; then
            # 3.1 清理 mbedtls 构建目录
            BUILD_DIR=$(find build_dir -type d -name "mbedtls-*" 2>/dev/null || true)
            if [[ -n "$BUILD_DIR" ]]; then
                rm -rf $BUILD_DIR
                echo "  ✓ 已清理 mbedtls 构建目录"
            fi
            
            # 3.2 修复 mbedtls Makefile
            sed -i 's/-Wno-unterminated-string-initialization//g' "$MBEDTLS_MAKEFILE"
            sed -i 's/-Wno-shift-count-overflow//g' "$MBEDTLS_MAKEFILE"
            sed -i 's/-Wno-array-bounds//g' "$MBEDTLS_MAKEFILE"
            
            if ! grep -q "CMAKE_OPTIONS.*-Wno-error" "$MBEDTLS_MAKEFILE"; then
                sed -i '/CMAKE_OPTIONS +=/ s/$/ -Wno-error/' "$MBEDTLS_MAKEFILE"
            fi
            
            sed -i 's/-U_FORTIFY_SOURCE -D_FORTIFY_SOURCE=0//g' "$MBEDTLS_MAKEFILE"
            sed -i '/CMAKE_OPTIONS +=/ s/$/ -U_FORTIFY_SOURCE -D_FORTIFY_SOURCE=0/' "$MBEDTLS_MAKEFILE"
            
            if ! grep -q "CMAKE_C_FLAGS" "$MBEDTLS_MAKEFILE"; then
                sed -i '/CMAKE_OPTIONS +=/a \ -DCMAKE_C_FLAGS="$(TARGET_CFLAGS) -U_FORTIFY_SOURCE -D_FORTIFY_SOURCE=0"' "$MBEDTLS_MAKEFILE"
            fi
            
            echo "  ✓ mbedtls Makefile 已修复"
        fi
        
        # 3.3 修复全局 hardened.mk
        if [[ -f "include/hardened.mk" ]]; then
            sed -i 's/-D_FORTIFY_SOURCE=1/-D_FORTIFY_SOURCE=0/g' include/hardened.mk
            sed -i 's/-D_FORTIFY_SOURCE=2/-D_FORTIFY_SOURCE=0/g' include/hardened.mk
            sed -i 's/-D_FORTIFY_SOURCE=3/-D_FORTIFY_SOURCE=0/g' include/hardened.mk
            echo "  ✓ include/hardened.mk 已修复"
        fi
        
        # 3.4 创建/更新 local.mk
        mkdir -p include
        if [[ -f "include/local.mk" ]]; then
            sed -i '/FORTIFY_SOURCE/d' include/local.mk
        fi
        echo "TARGET_CFLAGS += -U_FORTIFY_SOURCE -D_FORTIFY_SOURCE=0" >> include/local.mk
        echo "  ✓ include/local.mk 已更新"
        
        # 3.5 设置环境变量
        export EXTRA_CFLAGS="-U_FORTIFY_SOURCE -D_FORTIFY_SOURCE=0"
        export CFLAGS="-U_FORTIFY_SOURCE -D_FORTIFY_SOURCE=0"
        export CXXFLAGS="-U_FORTIFY_SOURCE -D_FORTIFY_SOURCE=0"
        echo "  ✓ 环境变量已设置"
        
        # ==========================================
        # 步骤 4：规范化配置
        # ==========================================
        echo ""
        echo "[步骤 4/4] 规范化配置..."
        
        make defconfig
        
        echo ""
        echo "=================================="
        echo "✓ 所有修复完成"
        echo "=================================="
    # 👆 修复步骤结束 👆

    - name: Download DL Package(下载DL软件包)
      run: |
        cd $OPENWRT_PATH
        make defconfig
        make download -j8
        find dl -size -1024c -exec ls -l {} \;
        find dl -size -1024c -exec rm -f {} \;

    - name: Compile Firmware(开始编译固件)
      id: compile
      run: |
        cd $OPENWRT_PATH
        echo -e "$(nproc) thread compile"
        make -j$(nproc) || make -j1 || make -j1 V=s
        echo "status=success" >> $GITHUB_OUTPUT
        echo "DATE=$(date +"%Y-%m-%d %H:%M:%S")" >> $GITHUB_ENV
        echo "FILE_DATE=$(date +"%Y.%m.%d")" >> $GITHUB_ENV

    - name: Check Space Usage(检查空间使用情况)
      if: (!cancelled())
      run: |
        df -hT

    - name: Upload Bin Directory(上传固件)
      if: steps.compile.outputs.status == 'success' && env.UPLOAD_BIN_DIR == 'true'
      uses: actions/upload-artifact@main
      with:
        name: ${{ env.SOURCE_REPO }}-bin-${{ env.DEVICE_TARGET }}-${{ env.DEVICE_SUBTARGET }}-${{ env.FILE_DATE }}
        path: ${{ env.OPENWRT_PATH }}/bin

    - name: Organize Files(整理文件)
      if: steps.compile.outputs.status == 'success'
      run: |
        cd $OPENWRT_PATH/bin/targets/*/*
        cat sha256sums
        echo "VERSION_KERNEL=$(find -type f -name "*.manifest" -exec grep -oP '^kernel - \K.*?(?=~|-\d+-)' {} \;)" >> $GITHUB_ENV
        cp $OPENWRT_PATH/.config build.config
        #mv -f $OPENWRT_PATH/bin/packages/*/*/*.ipk packages
        mkdir -p packages
        find "$OPENWRT_PATH/bin/packages/" -type f \( -name "*.ipk" -o -name "*.apk" \) -exec mv -f {} packages/ \; 2>/dev/null
        tar -zcf Packages.tar.gz packages
        rm -rf packages feeds.buildinfo version.buildinfo
        echo "FIRMWARE_PATH=$PWD" >> $GITHUB_ENV

    - name: Upload Firmware To Artifact(将固件上传到Artifact)
      if: steps.compile.outputs.status == 'success' && env.FIRMWARE_RELEASE != 'true'
      uses: actions/upload-artifact@main
      with:
        name: ${{ env.SOURCE_REPO }}-firmware-${{ env.DEVICE_TARGET }}-${{ env.DEVICE_SUBTARGET }}-${{ env.FILE_DATE }}
        path: ${{ env.FIRMWARE_PATH }}

    - name: Upload Firmware To Release(发布固件)
      if: steps.compile.outputs.status == 'success' && env.FIRMWARE_RELEASE == 'true'
      uses: ncipollo/release-action@v1
      with:
        name: R${{ env.DATE }} for ${{ env.FIRMWARE_TAG }}
        allowUpdates: true
        tag: ${{ env.FIRMWARE_TAG }}
        token: ${{ secrets.GITHUB_TOKEN }}
        artifacts: ${{ env.FIRMWARE_PATH }}/*
        body: |
          **This is OpenWrt Firmware for ${{ env.FIRMWARE_TAG }}**

          ### 📒 固件信息(无WIFI)
          - 无WIFI带有线NSS的6.12内核固件 基于B大源码的自用带集客AC固件 稳定性未知
          - 💻 这是 ${{ env.FIRMWARE_TAG }} 平台使用的 OpenWrt 固件
          - ⚽ 固件源码: ${{ env.REPO_URL }}
          - 💝 源码分支: ${{ env.REPO_BRANCH }}
          - 🌐 默认地址: 192.168.5.1
          - 🔑 默认密码: password

          ### 🧊 固件版本
          - 固件内核版本：${{ env.VERSION_KERNEL }}
          - 固件编译前最后一次➦[主源码](${{ env.REPO_URL }})更新记录 - ${{ env.VERSION_INFO }}
