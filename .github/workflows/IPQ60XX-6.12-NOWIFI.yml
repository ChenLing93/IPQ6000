name: IPQ60XX-6.12-NOWIFI

on:
  workflow_dispatch:
  schedule:
    - cron: 0 19 * * *

env:
  REPO_URL: https://github.com/laipeng668/openwrt-6.x.git
  REPO_BRANCH: main-nss
  CONFIG_FILE: configs/ipq60xx-6.12-nowifi.config
  DIY_SCRIPT: libwrt.sh
  CLASH_KERNEL: amd64
  CACHE_TOOLCHAIN: true
  UPLOAD_BIN_DIR: false
  FIRMWARE_RELEASE: true
  FIRMWARE_TAG: IPQ60XX-6.12-NOWIFI
  TZ: Asia/Shanghai

jobs:
  Build:
    runs-on: ubuntu-latest

    steps:
      - name: Check Server Performance(检查服务器性能)
        run: |
          echo "警告⚠"
          echo "分配的服务器性能有限，若选择的插件过多，务必注意CPU性能！"
          echo -e "已知CPU型号（降序）：7763，8370C，8272CL，8171M，E5-2673 \n"
          echo "--------------------------CPU信息--------------------------"
          echo "CPU物理数量：$(cat /proc/cpuinfo | grep "physical id" | sort | uniq | wc -l)"
          echo -e "CPU核心信息：$(cat /proc/cpuinfo | grep name | cut -f2 -d: | uniq -c) \n"
          echo "--------------------------内存信息--------------------------"
          echo "已安装内存详细信息："
          echo -e "$(sudo lshw -short -C memory | grep GiB) \n"
          echo "--------------------------硬盘信息--------------------------"
          echo "硬盘数量：$(ls /dev/sd* | grep -v [1-9] | wc -l)" && df -hT

      - name: Initialization Environment(初始化环境)
        env:
          DEBIAN_FRONTEND: noninteractive
        run: |
          sudo bash -c 'bash <(curl -sL https://build-scripts.immortalwrt.org/init_build_environment.sh)'
          sudo -E apt -yqq install dos2unix
          sudo -E apt -yqq install libfuse-dev
          sudo -E apt -yqq autoremove --purge
          sudo -E apt -yqq autoclean
          sudo -E apt -yqq clean
          sudo -E systemctl daemon-reload
          sudo timedatectl set-timezone "$TZ"

      - name: Combine Disks(合并磁盘)
        uses: easimon/maximize-build-space@master
        with:
          swap-size-mb: 1024
          temp-reserve-mb: 100
          root-reserve-mb: 1024

      - name: Checkout
        uses: actions/checkout@main

      - name: Clone Source Code(克隆源代码)
        run: |
          df -hT $GITHUB_WORKSPACE
          git clone --depth 1 -b $REPO_BRANCH $REPO_URL openwrt
          cd openwrt
          echo "OPENWRT_PATH=$PWD" >> $GITHUB_ENV
          VERSION_INFO=$(git show -s --date=short --format="作者: %an时间: %cd内容: %shash: %H")
          echo "VERSION_INFO=$VERSION_INFO" >> $GITHUB_ENV
          VERSION_KERNEL=$(grep -oP 'LINUX_KERNEL_HASH-\K[0-9]+\.[0-9]+\.[0-9]+' target/linux/generic/kernel-6.12)
          echo "VERSION_KERNEL=$VERSION_KERNEL" >> $GITHUB_ENV

      - name: Generate Variables(生成变量)
        run: |
          cp $CONFIG_FILE $OPENWRT_PATH/.config
          cd $OPENWRT_PATH
          make defconfig > /dev/null 2>&1
          SOURCE_REPO="$(echo $REPO_URL | awk -F '/' '{print $(NF)}')"
          echo "SOURCE_REPO=$SOURCE_REPO" >> $GITHUB_ENV
          DEVICE_TARGET=$(cat .config | grep CONFIG_TARGET_BOARD | awk -F '"' '{print $2}')
          echo "DEVICE_TARGET=$DEVICE_TARGET" >> $GITHUB_ENV
          DEVICE_SUBTARGET=$(cat .config | grep CONFIG_TARGET_SUBTARGET | awk -F '"' '{print $2}')
          echo "DEVICE_SUBTARGET=$DEVICE_SUBTARGET" >> $GITHUB_ENV

      - name: Cache Toolchain(缓存工具链)
        if: env.CACHE_TOOLCHAIN == 'true'
        uses: HiGarfield/cachewrtbuild@main
        with:
          ccache: false
          mixkey: ${{ env.SOURCE_REPO }}-${{ env.REPO_BRANCH }}-${{ env.DEVICE_TARGET }}-${{ env.DEVICE_SUBTARGET }}
          prefix: ${{ env.OPENWRT_PATH }}

      - name: Fix Missing Dependencies(修复缺失依赖)
        run: |
          cd $OPENWRT_PATH
          grep -q "CONFIG_PACKAGE_libparted=y" .config || echo "CONFIG_PACKAGE_libparted=y" >> .config
          grep -q "CONFIG_PACKAGE_boost-system=y" .config || echo "CONFIG_PACKAGE_boost-system=y" >> .config
          grep -q "CONFIG_PACKAGE_shadowsocks-libev-ss-local=y" .config || echo "CONFIG_PACKAGE_shadowsocks-libev-ss-local=y" >> .config
          grep -q "CONFIG_PACKAGE_shadowsocks-libev-ss-redir=y" .config || echo "CONFIG_PACKAGE_shadowsocks-libev-ss-redir=y" >> .config
          grep -q "CONFIG_PACKAGE_shadowsocks-libev-ss-server=y" .config || echo "CONFIG_PACKAGE_shadowsocks-libev-ss-server=y" >> .config
          grep -q "CONFIG_PACKAGE_python3-pysocks=y" .config || echo "CONFIG_PACKAGE_python3-pysocks=y" >> .config
          grep -q "CONFIG_PACKAGE_python3-unidecode=y" .config || echo "CONFIG_PACKAGE_python3-unidecode=y" >> .config

      - name: Install Feeds(安装feeds)
        run: |
          cd $OPENWRT_PATH
          ./scripts/feeds update -a
          ./scripts/feeds install -a

      - name: Load Custom Configuration(加载自定义配置)
        run: |
          [ -e files ] && mv files $OPENWRT_PATH/files
          [ -e $CONFIG_FILE ] && mv $CONFIG_FILE $OPENWRT_PATH/.config
          chmod +x $DIY_SCRIPT
          cd $OPENWRT_PATH
          $GITHUB_WORKSPACE/$DIY_SCRIPT

      - name: Comprehensive Fix for mbedtls and Toolchain(组合修复：mbedtls降级+工具链稳定)
        run: |
          cd $OPENWRT_PATH

          echo "🔧 开始组合修复：降级 mbedtls + 稳定工具链"
          echo "=========================================="

          # ==================== 方案一：降级 mbedtls ====================
          echo "📦 [1/2] 降级 mbedtls 版本..."

          if [ -f package/libs/mbedtls/Makefile ]; then
            # 备份原始 Makefile
            cp package/libs/mbedtls/Makefile package/libs/mbedtls/Makefile.bak

            # 显示当前版本
            echo "  当前 mbedtls 版本："
            grep -E "PKG_VERSION|PKG_SOURCE_VERSION" package/libs/mbedtls/Makefile | head -2

            # 降级到 3.5.2 版本
            sed -i 's/PKG_VERSION:=3.6.5/PKG_VERSION:=3.5.2/g' package/libs/mbedtls/Makefile
            sed -i 's/PKG_SOURCE_VERSION:=3.6.5/PKG_SOURCE_VERSION:=3.5.2/g' package/libs/mbedtls/Makefile

            # 验证修改
            echo "  修改后 mbedtls 版本："
            grep -E "PKG_VERSION|PKG_SOURCE_VERSION" package/libs/mbedtls/Makefile | head -2

            # 清理旧的构建目录
            rm -rf build_dir/target-aarch64_cortex-a53_musl/mbedtls-*
            rm -rf staging_dir/target-aarch64_cortex-a53_musl/usr/include/mbedtls
            rm -rf staging_dir/target-aarch64_cortex-a53_musl/usr/lib/libmbed*

            echo "  ✅ mbedtls 已降级到 3.5.2 版本"
          else
            echo "  ⚠️  未找到 mbedtls Makefile，跳过降级"
          fi

          echo ""

          # ==================== 方案三：使用稳定工具链 ====================
          echo "🔨 [2/2] 配置稳定工具链 (GCC 13.x)..."

          # 检查当前 .config 中的 GCC 配置
          echo "  当前 GCC 配置："
          grep -E "CONFIG_GCC_VERSION" .config || echo "  (未找到 GCC 配置)"

          # 添加或修改 GCC 版本配置
          # 首先禁用所有 GCC 版本
          sed -i '/CONFIG_GCC_VERSION_[0-9]/d' .config

          # 启用 GCC 13.x（如果可用）
          echo "CONFIG_GCC_VERSION_13=y" >> .config
          echo "CONFIG_GCC_VERSION_14=n" >> .config

          # 重新生成 defconfig 以应用配置
          make defconfig > /dev/null 2>&1

          # 验证修改
          echo "  修改后 GCC 配置："
          grep -E "CONFIG_GCC_VERSION" .config | head -2

          echo "  ✅ 已配置使用 GCC 13.x 工具链"

          echo ""
          echo "=========================================="
          echo "🎉 组合修复完成！"
          echo ""
          echo "修复内容："
          echo "  • mbedtls: 3.6.5 → 3.5.2"
          echo "  • 工具链: GCC 14.3.0 → GCC 13.x"
          echo "=========================================="

      - name: Deep Clean After Fix(修复后深度清理)
        run: |
          cd $OPENWRT_PATH

          echo "🧹 正在进行深度清理..."

          # 清理 mbedtls 相关的所有缓存
          rm -rf build_dir/target-aarch64_cortex-a53_musl/mbedtls-*
          rm -rf build_dir/host/mbedtls-*
          rm -rf staging_dir/target-aarch64_cortex-a53_musl/usr/include/mbedtls
          rm -rf staging_dir/target-aarch64_cortex-a53_musl/usr/lib/libmbed*
          rm -rf staging_dir/host/usr/include/mbedtls
          rm -rf staging_dir/host/usr/lib/libmbed*

          # 清理工具链缓存（如果使用了工具链缓存）
          rm -rf .ccache

          echo "✅ 深度清理完成"

      - name: Download DL Package(下载DL软件包)
        run: |
          cd $OPENWRT_PATH
          make defconfig
          make download -j8
          find dl -size -1024c -exec ls -l {} \;
          find dl -size -1024c -exec rm -f {} \;

      - name: Compile Firmware(开始编译固件)
        id: compile
        run: |
          cd $OPENWRT_PATH
          echo -e "$(nproc) thread compile"
          make -j$(nproc) || make -j1 || make -j1 V=s
          echo "status=success" >> $GITHUB_OUTPUT
          echo "DATE=$(date +"%Y-%m-%d %H:%M:%S")" >> $GITHUB_ENV
          echo "FILE_DATE=$(date +"%Y.%m.%d")" >> $GITHUB_ENV

      - name: Check Space Usage(检查空间使用情况)
        if: (!cancelled())
        run: df -hT

      - name: Upload Bin Directory(上传固件)
        if: steps.compile.outputs.status == 'success' && env.UPLOAD_BIN_DIR == 'true'
        uses: actions/upload-artifact@main
        with:
          name: ${{ env.SOURCE_REPO }}-bin-${{ env.DEVICE_TARGET }}-${{ env.DEVICE_SUBTARGET }}-${{ env.FILE_DATE }}
          path: ${{ env.OPENWRT_PATH }}/bin

      - name: Organize Files(整理文件)
        if: steps.compile.outputs.status == 'success'
        run: |
          cd $OPENWRT_PATH/bin/targets/*/*
          cat sha256sums
          echo "VERSION_KERNEL=$(find -type f -name "*.manifest" -exec grep -oP '^kernel - \K.*?(?=~|-\d+-)' {} \;)" >> $GITHUB_ENV
          cp $OPENWRT_PATH/.config build.config
          mkdir -p packages
          find "$OPENWRT_PATH/bin/packages/" -type f \( -name "*.ipk" -o -name "*.apk" \) -exec mv -f {} packages/ \; 2>/dev/null
          tar -zcf Packages.tar.gz packages
          rm -rf packages feeds.buildinfo version.buildinfo
          echo "FIRMWARE_PATH=$PWD" >> $GITHUB_ENV

      - name: Upload Firmware To Artifact(将固件上传到Artifact)
        if: steps.compile.outputs.status == 'success' && env.FIRMWARE_RELEASE != 'true'
        uses: actions/upload-artifact@main
        with:
          name: ${{ env.SOURCE_REPO }}-firmware-${{ env.DEVICE_TARGET }}-${{ env.DEVICE_SUBTARGET }}-${{ env.FILE_DATE }}
          path: ${{ env.FIRMWARE_PATH }}

      - name: Upload Firmware To Release(发布固件)
        if: steps.compile.outputs.status == 'success' && env.FIRMWARE_RELEASE == 'true'
        uses: ncipollo/release-action@v1
        with:
          name: R${{ env.DATE }} for ${{ env.FIRMWARE_TAG }}
          allowUpdates: true
          tag: ${{ env.FIRMWARE_TAG }}
          token: ${{ secrets.GITHUB_TOKEN }}
          artifacts: ${{ env.FIRMWARE_PATH }}/*
          body: |
            **This is OpenWrt Firmware for ${{ env.FIRMWARE_TAG }}**

            ### 📒 固件信息(无WIFI)
            - 无WIFI带有线NSS的6.12内核固件
            - 💻 这是 ${{ env.FIRMWARE_TAG }} 平台使用的 OpenWrt 固件
            - ⚽ 固件源码: ${{ env.REPO_URL }}
            - 💝 源码分支: ${{ env.REPO_BRANCH }}
            - 🌐 默认地址: 192.168.5.1
            - 🔑 默认密码: password

            ### 🧊 固件版本
            - 固件内核版本：${{ env.VERSION_KERNEL }}
            - 固件编译前最后一次➦[主源码](${{ env.REPO_URL }})更新记录
            - ${{ env.VERSION_INFO }}
